---
page_title: "viettelidc_ovpc_subnet Resource - viettelidc"
subcategory: "Virtual Private Cloud (OVPC)"
description: |-
  ViettelIDC subnet inside a VPC.
---

# viettelidc_ovpc_subnet (Resource)

ViettelIDC subnet inside a VPC.

## Example Usage

```terraform
resource "viettelidc_ovpc_vpc" "main" {
  name       = "my-vpc"
  cidr_block = "10.0.0.0/16"
}

resource "viettelidc_ovpc_subnet" "private" {
  name            = "private-subnet"
  network_address = "10.0.1.0/24"
  is_public_zone  = false
  vpc_id          = viettelidc_ovpc_vpc.main.id
  description     = "Private subnet"
}

resource "viettelidc_ovpc_subnet" "public" {
  name            = "public-subnet"
  network_address = "10.0.2.0/24"
  is_public_zone  = true
  vpc_id          = viettelidc_ovpc_vpc.main.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable subnet name.
- `network_address` (String) CIDR network address (e.g. 10.0.0.0/24). Immutable.

### Optional

- `description` (String) Optional description.
- `is_public_zone` (Boolean) Whether the subnet sits in the public zone. Immutable.
- `vpc_id` (String) VPC ID. Falls back to the provider default vpc_id when unset.

### Read-Only

- `id` (String) Subnet ID assigned by the system (vttSubnetId).
