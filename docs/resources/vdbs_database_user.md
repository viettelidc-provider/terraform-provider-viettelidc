---
page_title: "viettelidc_vdbs_database_user Resource - viettelidc"
subcategory: "Database Service (VDBS)"
description: |-
  ViettelIDC VDBS Database User — configures database users and schema permissions.
---

# viettelidc_vdbs_database_user (Resource)

ViettelIDC VDBS Database User — configures database users and schema permissions.

## Example Usage

```terraform
# Táº¡o Database User cho Instance
resource "viettelidc_vdbs_database_user" "app_user" {
  name        = "appuser12"
  password    = "Vtdc-123456"
  instance_id = "2851"
  host        = "%"
  schemas     = ["quannm"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `instance_id` (String) Database instance ID (can be numeric ID, UUID, or name).
- `name` (String) Database username.
- `password` (String, Sensitive) Database user password.

### Optional

- `host` (String) Host mask. Defaults to '%'.
- `schemas` (Set of String) Set of database schema names granted to this user.

### Read-Only

- `id` (String) Database user UUID assigned by the system.
