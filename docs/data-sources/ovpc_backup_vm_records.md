---
page_title: "viettelidc_ovpc_backup_vm_records Data Source - viettelidc"
subcategory: "Virtual Private Cloud (OVPC)"
description: |-
  List the VM backups a VPC holds, newest first — the records produced by viettelidc_ovpc_backup_scheduler. For volume backups (viettelidc_ovpc_backup_plan) use viettelidc_ovpc_backup_record instead.
---

# viettelidc_ovpc_backup_vm_records (Data Source)

List the VM backups a VPC holds, newest first — the records produced by viettelidc_ovpc_backup_scheduler. For volume backups (viettelidc_ovpc_backup_plan) use viettelidc_ovpc_backup_record instead.

## Example Usage

```terraform
# Every VM backup the VPC holds, newest first.
data "viettelidc_ovpc_backup_vm_records" "all" {
  vpc_id = data.viettelidc_ovpc_vpc.main.id
}

# Records a given schedule produced. scheduleName is empty for manual backups,
# so filtering on it is also how you tell the two apart.
output "daily_backups" {
  value = [
    for r in data.viettelidc_ovpc_backup_vm_records.all.records :
    { name = r.name, size = r.size, at = r.created_date }
    if r.schedule_name == viettelidc_ovpc_backup_scheduler.daily.name
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `vpc_id` (String) VPC filter; falls back to the provider default vpc_id.

### Read-Only

- `records` (Attributes List) (see [below for nested schema](#nestedatt--records))

<a id="nestedatt--records"></a>
### Nested Schema for `records`

Read-Only:

- `created_date` (String) Creation time, RFC 3339.
- `id` (String) Backup record UUID.
- `name` (String) Backup record name.
- `schedule_name` (String) Schedule that created the record; empty for a manual backup.
- `size` (Number) Backup size in bytes.
- `status` (String) Record status, e.g. "AVAILABLE".
- `vm_id` (String) Backup-service UUID of the VM, the same id viettelidc_ovpc_backup_vms returns.
- `vm_name` (String) Name of the VM that was backed up.
