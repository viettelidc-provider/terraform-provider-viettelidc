---
page_title: "viettelidc_ovpc_backup_vms Data Source - viettelidc"
subcategory: "Virtual Private Cloud (OVPC)"
description: |-
  List the VMs the backup service offers for a VPC, with the UUIDs that viettelidc_ovpc_backup_scheduler expects in vm_ids.
---

# viettelidc_ovpc_backup_vms (Data Source)

List the VMs the backup service offers for a VPC, with the UUIDs that viettelidc_ovpc_backup_scheduler expects in vm_ids.

## Example Usage

```terraform
# Every VM the backup service accepts for this VPC.
data "viettelidc_ovpc_backup_vms" "all" {
  vpc_id = data.viettelidc_ovpc_vpc.main.id
}

# Only the ones not already covered by a schedule.
data "viettelidc_ovpc_backup_vms" "unassigned" {
  filter = "unassigned"
  vpc_id = data.viettelidc_ovpc_vpc.main.id
}

output "backup_vm_uuid_by_name" {
  value = { for vm in data.viettelidc_ovpc_backup_vms.all.vms : vm.name => vm.id }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filter` (String) Which VMs to return: "all" (default) or "unassigned" — those not yet a member of any backup schedule.
- `vpc_id` (String) VPC filter; falls back to the provider default vpc_id.

### Read-Only

- `vms` (Attributes List) (see [below for nested schema](#nestedatt--vms))

<a id="nestedatt--vms"></a>
### Nested Schema for `vms`

Read-Only:

- `id` (String) VM UUID, as used in vm_ids.
- `name` (String) VM name.
