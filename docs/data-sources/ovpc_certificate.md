---
page_title: "viettelidc_ovpc_certificate Data Source - viettelidc"
subcategory: "Virtual Private Cloud (OVPC)"
description: |-
  Look up an existing ViettelIDC TLS/SSL Certificate by ID or name.
---

# viettelidc_ovpc_certificate (Data Source)

Look up an existing ViettelIDC TLS/SSL Certificate by ID or name.

## Example Usage

```terraform
data "viettelidc_ovpc_certificate" "tls" {
  name   = "my-tls-cert"
  vpc_id = data.viettelidc_ovpc_vpc.main.id
}

# load_balancers lists what is using the certificate. A cert in use cannot be
# deleted, so check this before removing one.
output "cert_in_use_by" {
  value = data.viettelidc_ovpc_certificate.tls.load_balancers
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) Certificate UUID. Either 'id' or 'name' must be provided.
- `name` (String) Certificate display name. Either 'id' or 'name' must be provided.
- `vpc_id` (String) VPC ID. Uses provider default if not specified.

### Read-Only

- `created_at` (String) Creation timestamp (ISO-8601).
- `load_balancers` (Attributes List) Load balancers currently using this certificate. A certificate in use cannot be deleted. (see [below for nested schema](#nestedatt--load_balancers))
- `status` (String) Current certificate status.

<a id="nestedatt--load_balancers"></a>
### Nested Schema for `load_balancers`

Read-Only:

- `id` (String) Load balancer ID.
- `name` (String) Load balancer name.
